version: '2.0'
services:

  mongo:
    container_name: mongo-1
    restart: always
    image: mongo:latest
    volumes:
      # move it!!
      - ./.mongo-data/live-data:/data/db
      - ./.mongo-data/backup-data:/backups
    ports:
      - 27017:27017
